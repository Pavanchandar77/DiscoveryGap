# Precomputed artifacts are large binaries — ship them via Git LFS so a fresh clone
# carries them and rank.py runs offline first-try (no re-precompute needed).
artifacts/*.npy filter=lfs diff=lfs merge=lfs -text
artifacts/*.parquet filter=lfs diff=lfs merge=lfs -text
